import { createContext } from "react";

export const AnswerContext = createContext(null);
export const PropsContext = createContext(null);
export const PrefsContext = createContext(null);
export const ColorContext = createContext(null);
export const SetColorContext = createContext(null);
export const GuessStateContext = createContext(null);